// Represents the layout of the widgets within the window. This will be passed to the rendering
// subsystem to actually render the primitives.
/*
struct LayoutGraph {
    nodes: Vec<SceneNode>,
}

struct SceneNode<'a> {
    calculated_layout: AbsoluteLayout,
    children: Vec<Node<'a>>,
}

pub trait LayoutEngine
{
    fn construct_layout_graph(root: &dyn Widget)
    {}
}

 */